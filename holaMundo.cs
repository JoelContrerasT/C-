// Importa el namespace System, que contiene funcionalidades esenciales como la clase Console
using System;

// Define una clase llamada Program, que será el contenedor principal de tu código
class Program
{
    // Este es el punto de entrada del programa: el método Main
    // Es el primer método que se ejecuta cuando inicias el programa
    static void Main(string[] args)
    {
        // Escribe un mensaje en la consola
        Console.WriteLine("Hola, Mundo");

        // Tarea practica
        Console.WriteLine("Hola, soy Joel Contreras. ¡Estoy aprendiendo C#!");
    }
}
